# Shared environment variables for all ttslo instances
# Copy this file to .env and fill in your actual values

# Telegram Bot Token (shared across all instances)
# Get this from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Note: Kraken API keys are set per-instance in docker-compose.yml
# Each instance can have different Kraken credentials
