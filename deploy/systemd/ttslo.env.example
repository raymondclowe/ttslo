# Example environment file for TTSLO service
# Copy to /etc/ttslo/ttslo.env and set permissions:
#   sudo install -o root -g ttslo -m 0640 ttslo.env /etc/ttslo/ttslo.env
# Or: sudo chown root:ttslo /etc/ttslo/ttslo.env && sudo chmod 0640 /etc/ttslo/ttslo.env

# Read-only Kraken credentials (required for monitoring)
KRAKEN_API_KEY=
KRAKEN_API_SECRET=

# Read-write Kraken credentials (optional; required to create orders)
KRAKEN_API_KEY_RW=
KRAKEN_API_SECRET_RW=

# App files (override defaults if desired)
TTSLO_CONFIG_FILE=/var/lib/ttslo/config.csv
TTSLO_STATE_FILE=/var/lib/ttslo/state.csv
TTSLO_LOG_FILE=/var/lib/ttslo/logs.csv

# Optional: tuning
# TTSLO_INTERVAL=60
